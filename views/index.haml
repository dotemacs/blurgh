
.title

  %h1
    %a{:href => '/'}= @title
  .subtitle 
    = @subtitle

- @posts.each do |post|
  .posttitle
    %a{:href => post.url}= post.title
  .date
    = days_ago(post.date)
  = parse(post.body)
